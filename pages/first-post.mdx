<!-- POST DETAILS -->

export const title = 'Hello World!';
export const author = 'Nurseiit';

import Details from '../utils/details';
export const details = new Details(title, author);

export const getInitialProps = () => ({ details });

<!-- END POST DETAILS -->

# Hello, world `awesome` :smile_cat:

```c++
#include <cstdio>

int main() {
  int a, b;
  scanf("%d%d", &a, &b);
  printf("%d\n", a + b);
}
```

Here's a paragraph

<img src="/static/img/loading.gif" alt="loading" style={{ height: '200px' }} />

Here's a table

| Test | Table |
| :--- | :---- |
| Col1 | Col2  |
